# files-kit (Alias) Agent Note

`packages/files-kit` is not implemented in this repository today.

- Real package: `packages/file-kit` (`@sheldrapps/file-kit`)
- Real agent rules: `packages/file-kit/AGENTS.md`

## TODO

- If the package is ever renamed from `file-kit` to `files-kit`, update:
  - `pnpm-workspace.yaml`
  - `tsconfig.base.json` paths
  - app `package.json` workspace dependencies
  - all imports currently using `@sheldrapps/file-kit`
