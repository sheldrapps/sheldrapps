# settings (Alias) Agent Note

`packages/settings` is not implemented in this repository today.

- Real package: `packages/settings-kit` (`@sheldrapps/settings-kit`)
- Real agent rules: `packages/settings-kit/AGENTS.md`

## TODO

- If the package is ever renamed from `settings-kit` to `settings`, update:
  - `pnpm-workspace.yaml`
  - `tsconfig.base.json` paths
  - app `package.json` workspace dependencies
  - all imports currently using `@sheldrapps/settings-kit`
