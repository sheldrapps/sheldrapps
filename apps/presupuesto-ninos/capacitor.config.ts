import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.sheldrapps.presupuestoninos',
  appName: 'Presupuesto Niños',
  webDir: 'www',
};

export default config;
